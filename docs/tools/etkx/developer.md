# etkx — developer guide

> This guide is a stub. Content is coming as the docs settle.

Things this guide will cover:

- Transbase → SQLite migration pipeline (JDBC drivers, schema mapping).
- Spring Boot service surface — endpoints, data model, query helpers.
- SvelteKit frontend wiring + dev proxy to the backend.
- Diagram rendering shared with `tisx`'s graphics format.
- Building from source + running the migration on your own ETK install.

In the meantime, the [project README](https://github.com/emdzej/etkx) has
the build / migration walkthrough.
