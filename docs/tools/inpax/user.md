# inpax — user guide

> This guide is a stub. Content is coming as the docs settle.

Things this guide will cover:

- Picking your INPA install folder (browser app).
- Browsing the script catalogue, pinning a startup script.
- Running an `.ipo` script and interacting with screens / menus / F-keys.
- Connecting over Web Serial vs a remote gateway.
- Headless / batch mode via the CLI.
- Reading the trace + log output for diagnostics.

In the meantime, the [project README](https://github.com/emdzej/inpax) has
quick-start instructions for both surfaces.
