{
  "name": "bimmerz",
  "scripts": {
    "preinstall": "npx only-allow pnpm", 
    "build": "npm run build --workspaces",
    "mqtt:start": "npm -w apps/mqtt start",
    "mqtt:develop": "npm -w apps/mqtt develop",
    "cli:start": "npm -w apps/simulator-cli start"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5"
  },
  "dependencies": {
    "tsx": "^4.19.0"
  }
}
